const { ActionRowBuilder, ButtonBuilder, ButtonStyle, SlashCommandBuilder, StringSelectMenuBuilder, StringSelectMenuOptionBuilder, ModalBuilder } = require('discord.js')
const modalService = require('../services/modalService')


module.exports = class Modal {
    constructor(customId, title, components, submitted) {
        //
    }
}